# schema.graphql
# ======================================================
# This schema defines the entities for your Subgraph.
# It is used to generate entitity files for your Subgraph.
# ======================================================

type User  {
  id: ID!
  username: String!
  name: String!
}
